Changement dans GamesState:next() pour explosion d'une bombe + changement ajout newlyDropped à newBombs et pas bombs

Changement dans GameState:nextBasts() pas exactement comme dans la consigne

Todo :


Pour modifier le nombre de players : (BONUS ?)
Dans PERMUTATIONS (de la classe GameState(server)), est-ce que cette liste est static ou est-ce qu'on la construit
dans le constructeur à partir des players ?
Dans XBlastComponent le drawing des scores des players est à revoir si on veut adapter le jeu pour plus de players
En fait tout le display ^^
PlayerPainter à adapter (avec les images et tout, pour si on a plus de 4 players)
(Level adapter la méthode defaultLevel())
Adapter le GameStateDeserializer (pour la création d'image du scoreboard)

BONUS :
- Music oklm
- Map Editor
- Affichage du winner sur le client (proposer de rejouer)
- Ajouter des bonus
- Changer le temps de la partie
- Le dernier joueur mort disparait et son nombre de vies passe à 0
- Bonus INVULNERABLE

Méthode dans BoardPainter pour mapEditor: (attention à changer en Byte pour réutiliser le runlengthencoder)

    /**
     * Returns the corresponding block image's ordinal of a given block
     * 
     * @param b
     *      The block 
     *      
     * @return
     *      The corresponding block image's ordinal of the given block
     * 
     */
    public int correspondingBlockImageOf(Block b){
        return palette.get(b).ordinal();
    }

